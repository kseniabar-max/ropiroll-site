{
  "name": "ropiroll-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node -e \"const fs=require('fs');const AdmZip=require('adm-zip');if(!fs.existsSync('server-logo.js'))new AdmZip('server-logo.zip').extractAllTo('.',true);import('./server-logo.js')\""
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "adm-zip": "^0.5.16",
    "express": "^5.1.0",
    "nodemailer": "^7.0.6",
    "pg": "^8.16.3"
  }
}
